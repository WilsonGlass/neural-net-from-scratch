<b>Loss/Cost</b>

If we take a softmax output of [.7, .1, .2] and
targets of [1, 0, 0], we can apply the calculations
as follows:

L_i = -(1 * log(.7) + 0 * log(.1) + 0 * log(.2))

$\therefore$ L_i = .35667

